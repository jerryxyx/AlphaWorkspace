# 2026‑03‑25 · Early‑Exercise Boundary Pattern Analysis & Premium Plots

## 🔍 Boundary Pattern Analysis (σ_spot = σ_fair)
**Objective**: Determine if call/put spreads exhibit one or two early‑exercise boundaries (finite range around strike) under varying volatility parameters.

**Parameter grid**:
- σ_fair ∈ [0.20,0.40] step 0.01 (21 values)
- σ_offset ∈ [0.00,0.10] step 0.01 (11 values)
- σ_spot = σ_fair (spot volatility equals implied volatility)
- S₀ = K = 100.0, T = 1.0, r = 0.05
- Lattice steps n = 50

**Method**: Fast inspection of exercise‑flag patterns from binomial tree at mid‑life (τ=0.5) and near expiry (τ=0.98).

**Findings**:
1. **Mid‑life (τ=0.5)**: **All 462 combos** (21×11×2) show pattern **“always”** – immediate unwinding optimal for every spot level. No finite boundary.
2. **Near expiry (τ=0.98)**:
   - 431 combos remain “always”.
   - **31 combos show “two_regions”** – exercise optimal at both high and low spots, with a small non‑exercise gap in between (e.g., spots >136.5 and spots <121.9).  
     → This is **not** a single contiguous range around the strike.
3. **Early‑exercise premium**: **Zero** for all combos – because S₀ = K lies inside the exercise region. American spread value equals European spread value.

**Conclusion**: Under equal spot and implied volatility, there is **no finite exercise boundary around the strike**; immediate unwinding is optimal for all spots. A finite contiguous range emerges only when σ_spot > σ_fair (higher spot volatility).

## 📄 Notion Documentation
Created page **[Boundary Analysis for Call/Put Spreads](https://www.notion.so/Boundary-Analysis-for-Call-Put-Spreads-32d271cfee8081dc988bcb449d747586)** under “Vol Margin Management” with icon 📊, containing full methodology, results summary, and key conclusions.

## 📈 Detailed Premium & Boundary Plots (σ_fair=0.2)
**Request**: Visualise premium curves and exercise regions for σ_spot ∈ [0.20,0.30] step 0.01 (11 values) and σ_offset ∈ [0.00,0.05] step 0.01 (6 values).

**Generated plots**:
- `premium_grid_call_put.png` – 11 rows (σ_spot) × 2 columns (call left, put right); each subplot shows premium vs spot for all σ_offset values.
- `boundary_heatmaps.png` – 2×2 heatmaps of lower/upper boundaries for call and put spreads.
- `exercise_regions_shaded.png` – shaded bands showing spot range where premium ≥ 0 for each σ_offset (call left, put right).

**Key visual insight**:  
- **Premium positive everywhere** for σ_offset > 0 → immediate unwinding optimal across all spots (exercise region = entire axis).
- **Finite range appears only when σ_offset = 0** (no volatility spread) – exercise region roughly [52.5, 147.5] (numerical zero‑crossing detection at grid edges).

**Boundary summary**:
| σ_offset | Call spread exercise region | Put spread exercise region |
|----------|----------------------------|----------------------------|
| **0.00** | [52.5, 147.5]              | [52.5, 147.5]              |
| **≥0.01**| **(-∞, ∞)** – always exercise | **(-∞, ∞)** – always exercise |

**Interpretation**: Under equal spot and implied volatility, a non‑zero volatility spread (σ_offset > 0) eliminates any finite exercise boundary. The option to wait has no value because the vega difference is already fully captured at all spot levels.

## 🧠 Learning
- **Exercise‑flag pattern analysis** is computationally efficient compared to brute‑force premium scanning across a fine spot grid.
- **Notion integration** provides a clean, structured repository for quantitative findings, enabling easy reference and sharing.
- **Visualisation** with parameter‑grid subplots and heatmaps effectively communicates the absence/presence of boundaries across multi‑dimensional parameter spaces.